#!/usr/bin/env bun
/**
 * Backward-compatible alias for the runtime-neutral host-tool example.
 * Runtime selection belongs to jsx-ai via JSX_AI_RUNTIME, not to this example.
 */
import "./runtime-agent";
